<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="timnop.srccalc.loncalcul">

    <uses-permission android:name="android.permission.INTERNET"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:name=".AdUnitsSampleApplication"
        android:largeHeap="true"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".second" />
        <activity android:name=".mortgagecalc" />
        <activity android:name=".loncalc" />
        <activity android:name=".Loancalcfinal" />
        <activity android:name=".listview" />
        <activity android:name=".homescreen" />
        <activity android:name=".creditdetail" />
        <activity android:name=".Aboutus" />
        <activity android:name=".MainActivity">

        </activity>
    </application>

</manifest>